# Environment Configuration for MCP MindMup Google Drive Server

# Environment (development/production)
ENV=development

# Google Drive API Configuration
GOOGLE_DRIVE_CREDENTIALS_FILE=/path/to/your/credentials.json

# MCP Server Configuration
MCP_SERVER_NAME=MindMup Google Drive Server
MCP_SERVER_VERSION=0.1.0

# HTTP Server Configuration (for Linux deployment)
MCP_MODE=http
MCP_HOST=0.0.0.0
MCP_PORT=9801

# Optional: Rate limiting and performance
MAX_CONCURRENT_REQUESTS=10
REQUEST_TIMEOUT=30

# Optional: Cache settings
CACHE_ENABLED=true
CACHE_TTL=300